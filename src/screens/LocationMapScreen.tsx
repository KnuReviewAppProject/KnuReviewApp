import React from 'react';
import { Text, View } from 'react-native';

const LocationMapScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'white',
      }}>
      <Text>Location Map Screen</Text>
    </View>
  );
}

export default LocationMapScreen